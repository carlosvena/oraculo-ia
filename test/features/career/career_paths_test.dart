import'dart:io';import'package:flutter_test/flutter_test.dart';import'package:oraculo_ia/src/features/career/domain/career_path.dart';void main(){test('eight paths prioritize Carlos goals',(){final p=parseCareerPaths(File('knowledge/career_paths_v1.json').readAsStringSync());expect(p,hasLength(8));expect(p.take(4).map((x)=>x.id),['advanced','automation','agents','app-builder']);expect(p.every((x)=>x.skills.isNotEmpty&&x.missions.isNotEmpty&&x.projects.isNotEmpty),isTrue);});}

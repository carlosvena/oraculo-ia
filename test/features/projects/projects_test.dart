import'dart:io';import'package:flutter_test/flutter_test.dart';import'package:oraculo_ia/src/features/projects/domain/learning_project.dart';void main(){test('five complete projects load',(){final p=parseProjects(File('knowledge/projects_v1.json').readAsStringSync());expect(p,hasLength(15));expect(p.every((x)=>x.steps.isNotEmpty&&x.deliverables.isNotEmpty&&x.success.isNotEmpty&&x.risks.isNotEmpty),isTrue);});}
